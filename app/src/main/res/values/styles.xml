<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/grey_500</item>
        <item name="colorPrimaryDark">@color/grey_700</item>
        <item name="colorAccent">@color/grey_500</item>
        <!-- 头像裁剪器-->
        <item name="cropImageStyle">@style/Widget.CropImageView</item>
        <!-- 图案解锁-->
        <item name="patternViewStyle">@style/PatternView</item>
    </style>

    <!-- 裁剪器-->
    <style name="Widget.CropImageView" parent="android:Widget">
        <item name="showThirds">true</item>
        <item name="showCircle">false</item>
        <item name="showHandles">always</item>
        <item name="highlightColor">#f3f3f3</item>
    </style>

    <!-- 选择Dialog-->
    <style name="Widget.SelectDialog" parent="Base.Theme.AppCompat.Dialog">
        <item name="android:backgroundDimEnabled">false</item>      <!-- 对话框出现时背景是否变暗-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@drawable/abc_popup_background_mtrl_mult</item>   <!-- 借用Spinner背景-->
        <item name="android:windowAnimationStyle">@style/Animation.SelectDialog</item>
    </style>
    <style name="Animation.SelectDialog" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_select_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_select_exit</item>
    </style>

    <!-- 图案解锁-->
    <style name="Base.PatternView" parent="">
        <item name="pl_aspect">square</item>
    </style>

    <style name="PatternView" parent="Base.PatternView">
        <item name="pl_regularColor">?colorControlNormal</item>
        <item name="pl_errorColor">#fff4511e</item>
        <item name="pl_successColor">@color/teal_500</item>
    </style>

    <style name="PatternView.Light" parent="Base.PatternView">
        <item name="pl_regularColor">?colorControlNormal</item>
        <item name="pl_errorColor">#fff4511e</item>
        <item name="pl_successColor">?colorControlActivated</item>
    </style>

</resources>
